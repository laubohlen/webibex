[pytest]
python_files = test_*.py
testpaths = core simple_landmarks
markers =
    spec(ref): links test to specification section
addopts = --cov=core --cov=simple_landmarks --cov=webibex --cov-report=term-missing
